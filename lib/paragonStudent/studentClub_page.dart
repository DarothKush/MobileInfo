import 'package:flutter/material.dart';

class StudentclubPage extends StatelessWidget {
  const StudentclubPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('StudentclubPage'),
      ),
      body: const Center(
        child: Text('StudentclubPage'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class StudentAmbassadorPage extends StatelessWidget {
  const StudentAmbassadorPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('StudentAmbassadorPage'),
      ),
      body: const Center(
        child: Text('StudentAmbassadorPage'),
      ),
    );
  }
}

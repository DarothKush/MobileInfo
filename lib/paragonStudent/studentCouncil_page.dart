import 'package:flutter/material.dart';

class StudentCouncilPage extends StatelessWidget {
  const StudentCouncilPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('StudentCouncilPage'),
      ),
      body: const Center(
        child: Text('StudentCouncilPage'),
      ),
    );
  }
}
